[project]
name = "validated-memory"
version = "0.1.0"
description = "Validated memory for agent projects: evidence states, supersession without deletion, freshness probes with a ternary verdict."
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]
