# X1 — the entry point

## Architect specification

Baseline: `89d068e`, V1 accepted. Review `cli.py`, `__main__.py`,
`__init__.py` and `probes/__init__.py`. Retain the argparse builder and
explicit dispatch: each command has distinct arguments, and a registration
layer would add an interface without simplifying the caller.

Changes are prose only. Preserve executable ASTs except docstrings, every
help/error string, argument and subcommand order, error precedence, versions
and test collection. Do not add speculative helpers or change dispatch.
Leave already concise files unchanged when they need no correction.

Condense repeated namespace/subparser explanations into the actual constraint:
cross-flag validation must use the selected command's usage line. Correct two
overclaims: `--as-of` selects a clock rather than gating on its own, and
conflicting resolution flags do not imply the transaction ID was unnamed.
Empty resolution IDs remain usage errors, not project findings.

Compare baseline/candidate stdout, stderr and exit codes for all help routes,
version, missing/unknown commands and an invalid-flag matrix, including
conflicting combinations that distinguish error precedence. Use the real CLI
and isolated adopter trees, never runtime imports. Run the full suite.

## Classification and scope

The exit-code summary is a contract. Subparser retention and validation order
are constraints; retain them concisely. The two overstated motivations are
replaced by the actual flag requirements, not moved into test prose.

Existing status tests assert exit 2 and the missing age-bound flag; the
journal resolution test asserts invalid combinations exit 2 and the journal
usage line. Remaining gaps include exact diagnostic wording, the status usage
line and competing-error precedence. The differential matrix provides
candidate evidence, not new permanent coverage. No test is added or weakened.

## Claude Code challenge

Claude Code Opus high accepted retaining the parser/dispatch. The architect
accepted its verification cautions: preserve tool directives, strip only real
leading docstrings, and compare help under the same interpreter, program name
and terminal width. Check docstring consumers before editing. Do not promote
source-size savings into a quota.

Namespace-held parser references are intentional; no caller serializes or
splats the namespace. Their asymmetry does not justify a new interface here.
Still-binding constraints stay concise beside the code under ADR 0010, rather
than moving every explanation into a new document. Documentary inaccuracies
within this pass are corrected; a new material runtime defect or unexplained
equivalence failure requires stopping and consulting the user.

## Verification

Only five comment groups in `cli.py` and the bundled-probes module docstring
changed. The version and entry-point files needed no edits. All four executable
ASTs match after removal of genuine leading docstrings; tool directives are
unchanged and no runtime docstring consumer was found.

The 102-case differential matrix compares stdout, stderr, exit code and fixture
tree after each command. It includes both help flags at the root and all eight
commands, version, missing/unknown commands and flags, twelve invalid status
cases and sixty invalid journal cases. Baseline and candidate use the same
interpreter, locale, width, hash seed and adopter directory. All cases match;
the tree remains unchanged. The reviewer reproduced this independently.

The programmer passed 661 tests in 73.37 seconds. Independent review approved
round 1 after 23 focused tests and corrected one inaccurate description of an
existing assertion in this report. No runtime correction was needed.

Across X1, source characters fell from 11,984 to 10,797 and prose from 1,775
to 759 (57.2%). LOC fell from 318 to 301; public/private definitions remain
2/0. No tests were added, deleted or renamed. The measured reduction is context
text, not runtime speed. Permanent diagnostic-coverage gaps remain documented.

The engineer returned PASS without edits: 661 tests in 70.77 seconds, the
102-case matrix reproduced, plus six independent competing-error cases over
a corrupt adopter tree. The architect reviewed the diff and evidence and
passed 661 tests in 73.14 seconds. X1 accepted on 2026-09-05; remote validation
follows. The published suite is 658 tests plus three clone-only local checks.
